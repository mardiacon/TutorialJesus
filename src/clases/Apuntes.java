package clases;

public class Apuntes {

	/* 
	 * ---IMPORTANTE TENER PRESENTE---
	 * 1. El código va de arriba a abajo
	 * 2. Todo lo que se muestra en pantalla va en un print
	 * 3. El código va saliendo poco a poco, no del tirón
	 * 4. Cada ciertas líneas de código imprimes por pantalla para ver si vas bien
	 */
	
	/*
	 * ---MÉTODOS---
	 * 2 tipos:
	 * 1. Metódos estáticos -> son los métodos de las clases (moldes).
	 * 2. Métodos no estáticos -> son los métodos de las instancias (objetos) (copias).
	 */
	
	/*
	 * ---CLASES Y OBJETOS---
	 * Para crear nuevos objetos se acude al constructor.
	 */
}
